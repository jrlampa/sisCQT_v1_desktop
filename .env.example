# Copie para `.env` e ajuste os valores para DEV.
# Nunca versione `.env` com segredos reais.

# --------------------------------------------
# Desenvolvimento (Docker Compose)
# --------------------------------------------
POSTGRES_USER=admin
POSTGRES_PASSWORD=password123
POSTGRES_DB=siscqt_db

# AI (Gemini) — usado pelo Theseus no Chatbot
GEMINI_API_KEY=

# Opcional (dev): mock auth com Bearer dev-token-im3
ENABLE_MOCK_AUTH=true

# --------------------------------------------
# Frontend (Vite) — OAuth (Desktop/Web)
# --------------------------------------------
# Entra ID (MSAL)
VITE_MSAL_CLIENT_ID=
VITE_MSAL_AUTHORITY=
# Google (GIS via @react-oauth/google)
VITE_GOOGLE_CLIENT_ID=

# --------------------------------------------
# Produção — API (Cloud Run / Azure / Docker prod)
# --------------------------------------------
# Obrigatórias em NODE_ENV=production (assertProdAuthConfig):
# MSAL_JWKS_URI=https://login.microsoftonline.com/<TENANT>/discovery/v2.0/keys
# MSAL_AUDIENCE=<client-id>
# MSAL_ISSUER=https://login.microsoftonline.com/<TENANT>/v2.0

# Database
# DATABASE_URL=postgresql://USER:PASS@HOST:5432/DB
# DIRECT_URL=postgresql://USER:PASS@HOST:5432/DB

# CORS: origens permitidas (vírgula). Desktop usa 28765.
# ALLOWED_ORIGINS=https://app.example.com,http://localhost:28765,http://127.0.0.1:28765

# Google login (opcional): validar ID token
# GOOGLE_CLIENT_ID=

# Proxy (1 ou true atrás de reverse proxy)
# TRUST_PROXY=1

# Rate limit (opcional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=300

# Readiness (opcional)
# READYZ_TIMEOUT_MS=2000
# READYZ_CHECK_POSTGIS=true

# Migrations no startup do container
# RUN_MIGRATIONS=true

# Stripe (billing)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# STRIPE_PRICE_ID_PRO=
# APP_BASE_URL=https://api.example.com

# --------------------------------------------
# Build Desktop (release) — GitHub Actions
# --------------------------------------------
# Secret VITE_API_BASE_URL_PROD: URL da API em produção.
# Ex.: https://api.suaempresa.com
# Definido em Settings → Secrets → Actions.
